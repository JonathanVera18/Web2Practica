import personajeRouter from './personaje.route'
import serieRouter from './serie.route'
import asignacionRouter from './asignacion.route'
import githubRouter from './github.route' // Asegúrate de que la ruta sea correcta

export {
    personajeRouter,
    serieRouter,
    asignacionRouter,
    githubRouter // Exporta la nueva ruta
}
